define([
        'jquery',
        'mage/url',
        'Magento_Checkout/js/model/full-screen-loader'
    ],
    function ($, Url, FullScreenLoader) {
        'use strict';

        return {
            test: function () {
                alert('i love you dad');
            }

 
        };
    }
);
